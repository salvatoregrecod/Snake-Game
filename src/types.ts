export enum Direction {
    Right = 1,
    Left,
    Up,
    Down
}

export interface Point {
    x: number,
    y: number
}

export enum Difficulty {
    Easy = 'easy',
    Medium = 'medium',
    Hard = 'hard'
}